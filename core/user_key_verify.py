# takes a file as input and verifies if the key inside is valid // if valid, allows access to the cloud database.
# level of access is determined by the access level in the file.
#  -> record ip address of user and attempted access time.