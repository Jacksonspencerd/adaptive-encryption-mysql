import os
import secrets
from typing import Optional
from pathlib import Path
import json

def generate_key(file_path: Optional[str] = None) -> bytes:
    """Generate a random access key based on level of access, save the key, user_id, and access level to a file if a path is provided (JSON format)."""
    pass 

